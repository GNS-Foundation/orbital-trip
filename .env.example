# Orbital TrIP — Environment Variables
# Copy to .env and fill in values

# Server
PORT=3000
NODE_ENV=development

# Railway (set in Railway dashboard, not here)
# RAILWAY_TOKEN=your_token_here

# CelesTrak API (no auth needed for basic access)
# CELESTRAK_BASE_URL=https://celestrak.org

# Space-Track.org (free account required for live data refresh)
# SPACETRACK_USER=your_email
# SPACETRACK_PASS=your_password
